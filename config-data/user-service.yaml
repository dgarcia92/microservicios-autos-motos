server:
  port: 8001
spring:
  application:
    name: user-service
eureka:
  client:
    fetch-regostry: true
    registerWithEureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
    instance:
      hostname: localhost