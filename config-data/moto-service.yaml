server:
  port: 8003
spring:
  application:
    name: moto-service
eureka:
  client:
    fetch-regostry: true
    registerWithEureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
    instance:
      hostname: localhost