server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/dgarcia92/microservicios-autos-motos.git
          search-paths: config-data 
  application:
    name: config-service